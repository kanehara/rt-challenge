import React from 'react';

const style = {
    footer: {
        position: 'absolute',
        bottom: '0',
        width: '100%',
        background: 'black'
    },
};

export default () => (
    <footer style={style.footer}>
    </footer>
);